/* Chat input and related elements */
#chat-input, #input-container, #chat-text-input, #add-smile, .custom-file-upload, #send-button, #send-button:hover {
    display: flex;
    align-items: center;
}

#chat-input {
    padding: 0.325rem 0.325rem;
    border-top: 3px solid #000;
    background: #656565; 
    background-color: #cecdcd; /* Set outer background to gray */
    height: 3rem; /* Set the height to match the send button */
    display: flex; /* Ensure flex container behavior */
    align-items: center; /* Center items vertically */
}

#input-container {
    flex-grow: 1;
    border-top: 2px solid #000;
    border-left: 2px solid #000;
    border-bottom: 2px solid #000;
    border-radius: 5px 0 0 5px;
    background: #fff; /* Set background to white */
    height: 2.75rem; /* Ensure it takes full height of parent */
    display: flex;
    align-items: center;
    transition: background-color 0.3s, border-color 0.3s; /* Smooth transition for hover effects */
}

#input-container:focus-within,
#input-container:hover {
    background-color: #e69500; /* Highlight color on hover or focus */
    border-color: #e69500; /* Change border color on hover or focus */
}

#chat-text-input {
    flex-grow: 1;
    padding: 0.625rem;
    width: 100%;
    border: none;
    outline: none;
    margin: 0;
    background: #fff; /* Set background to white */
    color: #000; /* Set text color to black */
    height: 100%; /* Ensure it takes full height of parent */
    box-sizing: border-box; /* Include padding and border in element's total width and height */
    cursor: text; /* Set cursor to text */
}

#chat-text-input::placeholder {
    color: #888; /* Set placeholder color to a visible gray */
}

#chat-text-input:focus {
    background: #f0f0f0; /* Change background to a light gray when focused */
    color: #000; /* Ensure text color is black */
    border: none; /* Remove border */
    outline: none; /* Remove outline */
    box-shadow: inset 0 0 0 2px #051b35; /* Add an inset shadow for focus indication */
}

#add-smile, .custom-file-upload {
    background: transparent;
    border: none;
    padding: 0;
    margin: 0 5px;
    cursor: pointer;
    font-size: 1.5rem; /* Increase font size for better appearance */
    display: flex;
    align-items: center;
}

#send-button {
    background-color: rgb(255, 94, 0); 
    color: white;
    border-top: 2px solid #000; 
    border-right: 2px solid #000; 
    border-bottom: 2px solid #000; 
    border-radius: 0 5px 5px 0; 
    padding: 0.625rem;
    max-width: 6.25rem;
    justify-content: center;
    display: flex;
    align-items: center;
    cursor: pointer;
    transition: background-color 0.3s;
    height: 3rem; 
    font-weight: bold;
    font-size: 16px;
}

#send-button:hover {
    background-color: #e69500; 
}

/* Close Chat Button */
#clear-chat {
    background-color: #000; 
    color: white; 
    border-radius: 3px; 
    cursor: pointer; 
    margin: 0.625rem 0;
    display: inline-block; 
    align-items: center;
    transition: background-color 0.3s;
}

#clear-chat:hover {
    background-color: #333; /* Set hover effect for Close Chat button */
    color: white;
    border-color: #e69500;
}
